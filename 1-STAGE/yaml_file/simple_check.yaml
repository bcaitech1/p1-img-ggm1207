program: train.py
method: grid
metric:
    goal: minimize
    name: valid_loss
parameters:
    test:
        value: true
    model_save:
        value: false
    train_key:
        values: ["age", "gender", "mask"]
